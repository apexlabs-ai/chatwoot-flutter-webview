name: chatwoot_flutter_webview
description: A new Flutter project.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  webviewx: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter